#include "stdafx.h"
#include "SfmlInputManager.hpp"

// Christopher D. Canfield
// March 2014
// SfmlInputManager.hpp

using namespace cdc;

SfmlInputManager::SfmlInputManager()
{
}


SfmlInputManager::~SfmlInputManager()
{
}


void SfmlInputManager::update(World& world)
{
	//using sf::Mouse

	//clonedEvent.mouseButton.x = static_cast<int>(window.mapPixelToCoords(location).x);
	//	clonedEvent.mouseButton.y = static_cast<int>(window.mapPixelToCoords(location).y);

	//if (Keyboard::isKeyPressed(
}